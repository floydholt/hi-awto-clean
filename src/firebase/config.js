// src/firebase/config.js

// ⬇️ Paste your existing config object here exactly as it used to be.
// For example:
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "hi-awto.firebaseapp.com",
  projectId: "hi-awto",
  storageBucket: "hi-awto.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};

// ❌ DO NOT initialize Firebase here.
// No initializeApp, no getAuth, no getFirestore, etc.
