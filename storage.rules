rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public read for listing images, writes only for authenticated users
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
