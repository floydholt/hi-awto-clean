{
  "name": "hi-awto-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for HI-AWTO (AI Vision, Pricing, Description, Fraud Detection)",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf lib",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0",
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
